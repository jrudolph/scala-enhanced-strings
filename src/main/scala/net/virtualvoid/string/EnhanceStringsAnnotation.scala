/**
 * The annotation is defined explicitly here, so that you can add the
 * plugin's jar file to your build dependencies if your IDE doesn't
 * support compiler plugins for presentation.
 */
class EnhanceStrings(syntax: String, version: Int) extends scala.annotation.Annotation
